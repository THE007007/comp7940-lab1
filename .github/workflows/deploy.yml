name: Deploy
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Deploy
        env:
          HEROKU_API_KEY: ${{secrets.HEROKU_API_KEY}}
          HEROKU_APP_NAME: "comp7940chatbo2023lmh" # Rename this to the app of your Heroku app.
        run: git push -f
https://heroku:$03e6b794-ae18-43c6-a016-3d6028790712@git.heroku.com/$comp7940chatbo2023lmh.git main